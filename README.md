to run:

flask server:
cd dynman-serve\
python3 -m venv venv\
source venv/bin/activate\
pip install -r requirements.txt\
python app\
\
dev server app in another terminal:\
cd dynman-app\
npm run dev\
